---
# Example SOPS-encrypted secret for feature/PR environments.
# See k8s/staging/secrets/drupal.sops.example.yaml for instructions.
apiVersion: v1
kind: Secret
metadata:
  name: drupal-project-secrets
type: Opaque
stringData:
  DRUPAL_HASH_SALT: "change-me-to-a-long-random-string"
